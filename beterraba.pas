Program cepodeMadeira ;
uses crt;
var
i, a, s, n, x: integer;
Begin
  {writeln('Quantas contas voce quer fazer, porra?');
  readln(n);
  clrscr;
  for i := 1 to n do
  begin
    writeln('Informe um numero');
    readln(a);
    clrscr;
    s := a + s;
    writeln('Faltam: ', n-i, ' numeros restantes');
    if (n-i = 0)
    then
    clrscr;
  end;
  
  writeln('A soma é: ', s);
  writeln;
  writeln('A media é: ', s/n:2:2);
  
  if (s <= 100)
  then
  writeln('Aumenta essa porra, carai')
  else
  writeln('Já tá de bom tamanho');}
  
  { writeln('Quantas pessoas existem no grupo');
	read(n);
	clrscr; 
	while (x <= n) do
		begin
		writeln('Digite as idades');
		readln(x);
		s := s + x;
		end;
		
	clrscr;
	writeln('A media das idades é: ', s/n:2:2); }
	
	
	
  
  
  
  
End.